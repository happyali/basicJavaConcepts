package Basics;

public class VariablesAndDatatypes {

	int data=50;			//instance variable  --A variable that is declared inside the class but outside the method is called instance variable . It is not declared as static.
	static int m=100;		//static variable  ----A variable that is declared as static is called static variable. It cannot be local.
	
	void method(){  
	int n=90;				//local variable  ----- A variable that is declared inside the method is called local variable.
	}  
	}						//end of class 
